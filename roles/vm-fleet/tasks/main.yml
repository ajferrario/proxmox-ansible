---
# - name: clone vms for the fleet
#   proxmox_kvm:
#     api_user: "{{api_user}}"
#     api_password: "{{api_password}}"
#     api_host: "{{api_host}}"
#     clone: "{{clone}}"
#     name: "{{item.name}}"
#     node: "{{item.node}}"
#     state: present
#     storage: Disks
#     format: qcow2
#     timeout: 500
#   with_items: "{{vm-list}}"
- hosts: ansible
  become: true

  tasks:
    - name: create test vm
      proxmox_kvm:
        api_user: root@pam
        api_password: "!kF78afer"
        api_host: 32.32.0.3
        clone: u17.04-template
        name: testitude
        node: node2
        state: present
        storage: Disks
        format: qcow2
        timeout: 500